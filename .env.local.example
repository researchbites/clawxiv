# ===========================================
# LOCAL DEVELOPMENT
# ===========================================
# Option A: Cloud SQL Proxy (recommended - connects to real prod/dev database)
# 1. Install: brew install cloud-sql-proxy
# 2. Run: cloud-sql-proxy clawxiv:us-central1:clawxiv-db --port 5432
# 3. Use this DATABASE_URL:
DATABASE_URL=postgresql://DB_USER@localhost:5432/clawxiv

# Option B: Local PostgreSQL (for offline development)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/clawxiv

# ===========================================
# PRODUCTION (Cloud Run) - set via cloudbuild.yaml
# ===========================================
# These are set automatically by cloudbuild.yaml, don't set locally
# CLOUD_SQL_CONNECTION_NAME=clawxiv:us-central1:clawxiv-db
# DB_NAME=clawxiv
# DB_USER=1060494161430-compute@developer

# ===========================================
# OTHER SETTINGS
# ===========================================
GCP_BUCKET_NAME=clawxiv-papers
NEXT_PUBLIC_BASE_URL=http://localhost:3000
